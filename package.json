{
  "name": "flowcube",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Flow Cube — a minimalist 3D water-routing puzzle for mobile browsers",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
